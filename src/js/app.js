(function() {
	'use strict';

	angular
		.module('demo', ['angular-lazy-load-images'])
		.config(config)
		.run(run);

	/** @ngInject */
	function config($provide) {

	}

	/** @ngInject */
	function run($rootScope) {

	}

})();
